# Changelog

## Z80Z-chat v1.0.29（2026-08-05）

好友列表与成员列表优化。

### 修复

- **好友请求方向区分**：`/friends/list` 新增 `direction` 字段（outgoing/incoming），修复"我发起的请求也显示同意按钮（点击必 403）"的问题——收到的请求显示 同意/拒绝，已发送请求显示 等待对方同意/取消

### 改进

- **好友页**：搜索框加图标（与登录页风格统一）、搜索结果面板进入动画、好友行点击打开用户资料弹窗（复用 UserProfileModal）、空状态加图标
- **成员栏**：语音中成员行尾加状态图标、自己的行显示"（你）"标记、离线成员头像灰度化、三个分组标题加计数徽章

### 测试

- 新增 2 个好友测试（direction 方向断言），共 108 个用例

## Z80Z-chat v1.0.28（2026-08-05）

前端美术优化（保持 Discord 暗色风格与布局，纯视觉改动）。

### 视觉优化

- **登录/注册页重设计**：渐变氛围光晕背景（`.auth-bg` 双光球漂移动画）、卡片强化（描边 + 深投影 + 半透明）、渐变品牌 logo（发光环）、输入框内嵌图标、渐变主按钮（悬浮提亮 + 按压缩放）、元素交错进入动画
- **全局主题**：CJK 优先字体栈（PingFang SC / 微软雅黑，修复中文基线偏移）、`::selection` 主题色、`:focus-visible` 焦点环、滚动条统一迁移、`prefers-reduced-motion` 减弱动态
- **主界面细节**：消息行进入动画（`message-in`）+ 间距/hover 优化、输入区圆角 + 聚焦光环 + 图标按钮按压反馈、表情面板弹出动画、频道/成员项 hover 层次、服务器栏 Discord 风格白色指示条（hover 20px / active 36px 高度过渡）
- **弹窗与菜单**：modal-overlay 淡入 + modal-content 缩放进入（7 个弹窗组件零改动自动生效）、上下文菜单缩放动画（右上角原点）
- **移动端**：底部导航按压缩放反馈、抽屉缓动

### 验证

- 新增 `frontend/scripts/check-art.mjs`：构建后断言 16 项美术约定（字体/焦点环/关键帧/auth 类/弹窗动画等）
- 浏览器实测（Edge 无头）：登录/注册/主界面/创建服务器弹窗/频道切换/消息发送全链路正常，106 个单元测试全绿

## Z80Z-chat v1.0.27（2026-08-05）

安全加固与性能优化（基于全面代码审查修复）。

### 安全修复

- **修复存储型 XSS（严重）**：消息内容渲染前先 HTML 转义再高亮 @提及（`frontend/src/utils/escape.ts`），杜绝用户消息注入脚本窃取 token
- **修复语音信令注入**：WebSocket 信令转发（offer/answer/ICE/投屏刷新）前校验发送者是语音房间成员，非成员无法向房间内用户注入伪造信令
- **修复角色层级越权**：`remove-role` 补齐 `canManageRole` 层级校验（与 `set-role` 一致），低层级管理员无法移除高层级角色
- **上传文件访问签名**：`/uploads` 由无鉴权静态托管改为 HMAC 签名访问（7 天有效期，`?expires=&sig=`），无签名/篡改/过期一律 403，杜绝路径穿越
- 签名密钥自动生成持久化到 `data/.upload-secret`，可在 config.json 配置 `uploadSecret` 固定密钥、`uploadUrlExpiresSec` 调整有效期

### 性能优化

- **消除 N+1 查询**：消息列表、成员列表、服务器详情批量解析用户（`findUsersMap` 单次遍历建 Map），替代循环内逐条 `findUser`

### 代码质量

- 提取公共下载工具 `utils/download.js`（cloudflared/frpc 下载去重，带进度与失败清理）
- 提取防火墙 UAC 提权辅助 `runElevatedPs1`（应用/删除规则共用）
- 日志控制台输出统一为 `[时间] [级别] 消息` 格式（与文件格式一致）

### 测试

- 新增 25 个用例（共 106 个）：XSS 转义、信令隔离、角色层级、签名访问控制、下载工具、控制台日志格式

## Z80Z-chat v1.0.26（2026-08-05）

清理文档与演示页的修饰性内容。

### 改进

- 投屏全屏功能修复说明、投屏显示模式演示：移除标题/列表装饰 emoji、营销式措辞（"更好的视觉体验"、"沉浸式体验"等），改为平实描述
- voice.ts 注释"天花板"改为"上限"
- 功能性内容保留：emoji 选择器表情列表、演示页 ✅/❌ 对比表

## Z80Z-chat v1.0.25（2026-08-05）

项目清档扩展：一并清除隧道配置。

### 改进

- 清档删除项新增隧道配置（Cloudflare `cloudflared/settings.json`、`config.yml`、`.cloudflared.pid`；SakuraFRP `settings.json`、`frpc.ini`、`.sakurafrp.pid`、`frpc.log`）
- 清档前自动停止运行中的隧道（Cloudflare / SakuraFRP）
- 隧道程序本体（cloudflared.exe / frpc.exe）保留，避免重新下载；界面已注明

## Z80Z-chat v1.0.24（2026-08-05）

SakuraFRP 国内低延迟穿透集成 + 智能快速更新。

### 新增

- 「隧道管理」改为服务选择页：`1. Cloudflare Tunnel` / `2. SakuraFRP`（国内节点，低延迟）
- SakuraFRP（樱花内网穿透）完整集成：
  - 首次配置向导：安装 frpc（粘贴面板账号专属下载链接）→ 访问密钥 → 创建隧道指引（自动打开面板）→ 连接配置
  - 两种连接模式：交互模式（运行 frpc TUI 勾选隧道，生成 frpc.ini）与命令行模式（`frpc -f 密钥:隧道ID`）
  - 启动/停止/状态/测试公网访问：后台运行 + PID 记录 + 日志重定向 + 从日志自动解析访问地址
  - frpc 下载到外层 `sakurafrp/` 目录（frpc.exe / frpc.ini / settings.json / frpc.log），与 cloudflared 同模式
- 智能快速更新：内容指纹比对（PAYLOAD_SHA，zip 剔除 package.json 的 sha256）
  - 启动器内容未变化时跳过备份 / npm install / 构建，仅解压覆盖 + 写版本标记，秒完成
  - 判定条件：指纹一致 + 依赖（dependencies/devDependencies，排除 version）一致 + node_modules/dist 存在
  - 首次 v1.0.23 → v1.0.24 为完整更新（旧版无指纹记录），同时写入 payloadHash 供下次使用

### 修复

- 脚本被误以错误编码重写后从构建产物恢复 demo-start.js（GBK/UTF-8 编码事故处理）

## Z80Z-chat v1.0.23（2026-08-04）

cloudflared 下载页镜像 Node.js 安装向导流程。

### 改进

- 安装/修复页面重做：说明页（为什么需要 cloudflared？）→ 选项（自动下载 / 手动下载教程 / 返回）
- 自动下载前新增「网络线路选择」：HEAD 测速 + 颜色分级（绿 <50ms / 黄 <150ms / 红超时），留空自动选最快
- 线路源：GitHub 官方 / ghproxy.net / gh-proxy.com；下载带进度显示，完成后 `--version` 校验，输出安装位置
- 下载失败后按回车重新选择线路下载（与 Node.js 流程一致）
- 手动下载教程改为三步指引：下载 / 重命名 / 放入目录（分隔线分步）
- 文案修正：约 70MB → 约 54MB（实测下载体积）

## Z80Z-chat v1.0.22（2026-08-04）

Cloudflare Tunnel 公网访问集成：无公网 IP 的私人电脑也能通过域名访问 Z80Z-chat。

### 新增

- 主菜单新增「隧道管理」入口（运行中/未运行菜单均有）
- 子菜单：首次配置向导 / 启动隧道 / 停止隧道 / 安装修复 cloudflared / 测试公网访问 / 状态一览
- cloudflared 与 Node.js 同模式管理：不内嵌二进制，按需下载到外层 `cloudflared/` 目录
  - 多源自动切换：官方 GitHub → ghproxy.net → gh-proxy.com → 手动下载教程兜底
  - 下载带进度显示，完成后自动校验版本
- 首次配置向导：`tunnel login`（浏览器授权）→ 创建/复用隧道 → `route dns` 绑定子域名 → 自动生成 config.yml → 可立即启动
- 隧道后台运行 + PID 记录；config.yml 每次启动按当前 config.json 端口重写
- 公网可达检测：启动后自动测试 `https://chat.z80z99.cn` 是否生效
- 子域名默认 `chat.z80z99.cn`，可在 `cloudflared/settings.json` 的 `hostname` 字段自定义

## Z80Z-chat v1.0.21（2026-08-04）

切换版本功能：在外层已安装的多个版本之间切换。

### 新增

- 主菜单新增「切换版本」（未运行 7 / 运行中 8）
- 列出外层所有已安装项目（版本号 + 安装时间 + [当前] 标记）
- 切换前自动停止当前运行的服务；切换后写入 bat 偏好 LAST_PROJECT，重新运行 bat 生效
- 等长写入：start.js 的 setBatPreference() 与引导层 PAD 基准保持一致

## Z80Z-chat v1.0.20（2026-08-04）

修复更新后自动改名与模板恢复的 PAD 基准问题。

### 修复

- 自动改名改用正则提取文件夹名自身版本尾缀（修复标记版本与文件夹名不一致时的错位场景）
- restore-template.ps1 恢复模板时强制重置 PAD=71，防止运行过的 bat 把错位基准带回模板
- 更新完成后文件夹自动重命名（如 z80z-chat-1.0.17 → z80z-chat-1.0.20）

## Z80Z-chat v1.0.19（2026-08-04）

更新后自动重命名项目文件夹（带版本号尾缀）。

### 新增

- 更新完成后自动将项目文件夹重命名为新版本（同步更新 LAST_PROJECT）
- 边界处理：目标已存在 / 文件夹名无版本尾缀 / 重命名失败时给出提示

## Z80Z-chat v1.0.18（2026-08-04）

安装向导界面文本全面润色。

### 改进

- 删除 26 处 Prompt 提示与 Read-Host 重复输出
- 清理 18 处意义不明的分隔线
- 修复全角逗号、版本未知、翻译腔、刷新间隔文案等文本问题

## Z80Z-chat v1.0.17（2026-08-04）

修复 config.json 重复键导致的配置失效问题。

### 修复

- Set-ConfigField 重写：MatchEvaluator + hashtable 闭包计数去重，同一键只保留最后一项
- 单元测试 11/11 通过

## Z80Z-chat v1.0.16（2026-08-04）

### 新增

- 端口随机可用检测、Node.js 多镜像源下载（官方/华为云/腾讯云/中科大）
- Z80Z-chat-DEMO.bat 自包含演示版（零副作用菜单模拟）

## Z80Z-chat v1.0.15（2026-08-03）

优化"直接使用现有目录"的体验，消除误报的首次部署界面。

### 改进

- `isFirstDeploy` 判定增强：无 `.install-version` 但依赖完整（node_modules + dist 存在）的目录
  （如开发源码目录）不再进入"首次部署"界面，直接进入管理菜单，并提示未登记安装标记
- "严重警告"收窄：仅在 data/ 目录存在且 node_modules 缺失时显示（真·数据风险）；
  纯源码目录/依赖完整目录不再误报"文件缺失"
- 警告文案更准确：明确指出缺失内容（data/ 存在但缺少 node_modules/）
- 未登记目录进入管理菜单前显示一行说明（为什么每次运行 bat 需要确认该目录）

## Z80Z-chat v1.0.14（2026-08-03）

多版本共存与更新偏好管理。

### 新增

- 安装时项目文件夹默认带版本号：`z80z-chat-1.0.14`（可自定义，留空即默认）
- 多版本共存：可同时安装多个版本（各自独立目录、独立数据）
- 版本选择界面：检测到新版本时选 N 进入，或已设"不再提示"后从精简界面进入
  - 列出所有已安装版本（版本号 + 安装时间 + [当前] 标记）
  - 切换后记录为默认使用项目，下次双击自动进入该版本
- "不再提示更新"（S 选项）：持久化到 bat 自身配置区，下次检测到新版本不再询问
  - 之后显示精简界面：切换版本 / 进入管理 / 恢复更新提示
- bat 内置偏好配置区（`REM _CONFIG_HASH=_SKIP_UPDATE_PROMPT=_SKIP_UPDATE_PROMPT_VERSION=_LAST_PROJECT=`）
  - 零额外文件，配置跟随 bat 自动迁移
  - 修改时哈希校验 + 自动备份 + 失败恢复

### 改进

- 多项目发现时优先使用 bat 记录的 lastProject，无需每次手动选择
- 更新界面：`Y=更新 / N=切换版本 / S=不再提示`，输入提示更清晰

## Z80Z-chat v1.0.13（2026-08-03）

优化 bat 所有页面风格统一，增强易用性。

### 改进

- 统一页面布局：H1 标题 + 当前状态 + 分隔符 + 选项 + 帮助信息 + 输入提示
- 新增样式函数：
  - `ItemWithDesc()`：带描述的选项（灰色描述缩进显示）
  - `Info()`：普通信息提示（蓝色）
  - `Prompt()`：输入提示（黄色）
  - `Progress()`：进度提示（绿色）
  - `Separator()`：分隔线
  - `KeyValue()`：键值对展示
- 网络线路选择：延迟按速度分级颜色（绿色<50ms，黄色<150ms，橙色其他）
- 端口配置：显示当前端口（青色高亮）、添加端口范围说明
- 安装/更新流程：详细进度提示、版本信息展示（KeyValue）、操作前说明
- Node.js 安装引导：选项添加描述、帮助信息格式化
- 警告界面：目录列表显示文件数、多步骤操作添加分隔符
- 错误处理：统一错误页面格式（标题 + 错误信息 + 分隔符 + 堆栈跟踪 + 输入提示）
- 所有输入提示统一格式（Prompt 函数）

## Z80Z-chat v1.0.12（2026-08-03）

首次安装界面支持"不安装，直接使用现有目录"。

### 变更

- 检测到未安装的项目目录时提供 3 选项：继续安装 / 直接使用现有目录（进入项目管理，可原地安装依赖）/ 退出
- `installAndBuild` 成功后写入 `.install-version`（版本同源于 package.json），下次运行 bat 自动发现进入主菜单
- 修复：node 直接 spawn 子进程时，父进程工作目录含空格（如 `E:\Opencode Project`）会导致 vite 构建失败——npm install / build 改经 PowerShell 中间层执行
- 修复：外层纯净度检测忽略运行期存在的 `.z80z-chat-bootstrap.ps1`（避免误报"第一次安装建议"）

## Z80Z-chat v1.0.11（2026-08-03）

首次安装误判防护：避免"部署目录内已有项目/源码文件夹仍进入首次安装"。

### 变更

- 安装前检测"含项目文件（start.js）但无安装标记"的目录（源码目录/未完成安装），警告并需确认后才继续
- 目标项目文件夹已存在且非空时二次确认（防止覆盖已有内容）
- 任一确认取消即退出，不产生任何改动

## Z80Z-chat v1.0.10（2026-08-03）

引导层安装流程改为"每步独占一屏"（与项目管理菜单一致）。

### 变更

- 新增 `Clear-Screen`：每个步骤标题（H1）开始前清屏
- 重定向/管道运行（自动化）时自动跳过清屏，避免输出污染

## Z80Z-chat v1.0.9（2026-08-03）

修复 PowerShell 5.1 下 ANSI 转义失效（彩色输出显示为裸文本 `e[38;5;69m`）。

### 修复

- 引导层 `Tone` 改用 `$ESC = [char]27` 生成转义序列（原 `` `e `` 转义仅 PS 6+ 支持，Windows 自带 PS 5.1 会输出字面字母 `e`）
- 输出重定向（管道/自动化）时自动降级无色（`[Console]::IsOutputRedirected` 检测）

## Z80Z-chat v1.0.8（2026-08-03）

单文件部署配置收敛：外层 `.install-config.json` 取消，全部部署配置并入项目 `config.json`。

### 变更

- 安装完成后不再生成外层 `.install-config.json`（旧版残留会在安装/更新时自动吸收并删除）
- 项目目录改为自动发现（按 `.install-version` + `start.js`），不再依赖外层记录的项目名；多项目时列出选择
- 部署配置统一承载于 `config.json`：新增 `backupKeep`（备份保留份数）、`nodeVersion`、
  `nodeMirror`、`npmRegistry` 字段（带中文注释），备份管理与 npm 安装从项目配置读取
- 更新流程保留 `config.json`（用户端口/镜像/备份设置不丢失，解压时暂存-移回）
- 单文件模式判定改为外层存在 `Z80Z-chat.bat`
- 新增 `docs/SINGLE-FILE.md` 单文件部署文档

## Z80Z-chat v1.0.0（2026-08-02）

Discord 风格社区聊天系统的首个正式发布版本。

### 新增功能

- 完整的社区聊天体系：服务器 / 频道 / 分类 / 角色 / 成员管理
- 实时消息：REST API + WebSocket 推送，消息编辑/删除实时同步
- 语音频道：WebRTC 点对点语音，麦克风 / 扬声器 / 音量控制
- 屏幕投屏：画质可选，手机可观看，黑屏自动修复
- 好友系统：添加 / 接受 / 删除，邀请链接加入服务器
- 文件上传：图片与附件，类型白名单 + 大小限制
- 移动端适配，自动刷新与断线重连

### 安全修复

- **角色批量赋值漏洞**：`PUT /roles/:id` 增加字段白名单，禁止篡改 `id`/`serverId`/`isDefault`
- **禁言与权限生效**：消息发送检查禁言状态、`sendMessage`/`viewChannel`/`mentionEveryone` 权限（REST 与 WS 一致）
- **Session 生命周期**：30 天过期自动清理，过期 token 立即失效
- **时序攻击防护**：登录对不存在的用户名执行假 bcrypt 比较
- **WebSocket 权限**：`join-channel` / `voice-join` 校验频道存在性、成员身份与 `viewChannel` 权限，广播隔离
- **角色层级**：仅能修改/删除/分配低于自身最高角色的角色，owner 豁免
- **好友请求方向校验**：仅被请求方可接受请求
- **上传白名单锚定**：扩展名必须完全匹配，杜绝 `tjpeg` 等子串绕过
- **邀请码脱敏**：普通成员不可见邀请码，仅 owner / manageServer 可见
- **附件清理**：删除消息/频道/服务器时清理无引用附件文件
- **API 错误规范化**：全局错误处理，404/400/413/500 统一 JSON

### 性能优化

- 消息排序改用 ISO 字符串字典序，消除 `new Date` 分配
- 消息发送/广播逻辑收敛为共享模块（REST 与 WS 单一实现）
- 权限检查统一为单模块，删除 5 处重复实现
- 启动横幅动态对齐

### 可靠性

- **原子数据库写入**：临时文件 + fsync + rename，写入中断不损坏 db.json
- **备份恢复**：`npm run backup` / `node scripts/restore.js`，恢复前自动保全现场
- **日志系统**：app.log / error.log 分离，5MB 自动轮转，写入失败静默
- **优雅关闭**：SIGINT / SIGTERM / SIGBREAK，通知 WS 客户端后安全退出
- **自动化测试**：81 个用例（node:test），覆盖认证/权限/消息/WS/语音/上传/备份/日志

### 部署支持

- Windows：`start.bat` 交互式管理菜单（启动/重启/停止/后台/清档/备份）
- Linux：systemd 服务模板（独立用户、自动重启、UMask=0077 安全加固）
- 环境变量：`PORT` / `HOST` / `DATA_DIR` / `LOG_DIR` 全部可配置
- 反向代理：nginx / Caddy / Cloudflare Tunnel 兼容（前端自动适配 HTTPS/WSS）
- 运维文档：DEPLOYMENT / BACKUP / TESTING / TROUBLESHOOTING

### 已知限制

- 存储为单文件 JSON（本地应用规模），消息量大时全量写盘为性能上限
- 频道级权限覆盖（`channel.permissions`）尚未实现，频道可见性由服务器级 `viewChannel` 控制
- 无内置 TLS（需反向代理），无速率限制（公网部署建议加反代限流）
- `start.js` 管理工具为 Windows 专属，Linux 使用 systemd 管理
- 数据库不支持跨版本迁移（无 schema 版本字段），升级前请先备份
